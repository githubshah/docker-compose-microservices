version: "3"
services:
  consumer:
    image: employee-consumer
    networks:
      - consumer-producer
    depends_on:
      - producer1
 
  producer1:
    image: employee-producer
    ports:
      - "8080:8080"
    networks:
      - consumer-producer 

networks:
  consumer-producer: